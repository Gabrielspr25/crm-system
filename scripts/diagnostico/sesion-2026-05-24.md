# Sesión de trabajo — VentasProui CRM
**Fecha:** 2026-05-24  
**Participantes:** Gabriel Sánchez / Claude  

---

## 1. Contexto de arranque

Esta sesión continuó desde una conversación anterior donde se había:
- Corrido el script `reporte-conteos.sql` en producción y obtenido los conteos reales de la BD
- Confirmado que los 16 clientes en el tab Seguimiento son la fuente de verdad de Mi Día
- Acordado borrar `crm_deals` (75 filas) y `crm_deal_tasks` (629 filas) para empezar limpio

---

## 2. Terminología unificada

**Decisión:** renombrar "Pendientes" → "Pasos" en toda la UI para hablar un solo idioma.

**Archivos modificados:**

| Archivo | Cambios |
|---|---|
| `src/react-app/pages/FollowUp.tsx` | Tab label, comentarios, botón "Nuevo paso", textos de carga/vacío, mensaje de confirmación |
| `src/react-app/pages/Clients.tsx` | Tab label, título/subtítulo del panel, botón "Nuevo Paso", textos vacío/carga, "Guardar Paso" |

**Lo que NO se renombró:** contadores de "tareas pendientes" en Director, Home y directorController — son KPIs genéricos de carga de trabajo, no el concepto de pasos del workflow.

---

## 3. Arquitectura limpia definida (pasos)

### Lo que se elimina
- `crm_tasks` — tabla, API `/api/tasks`, componentes PendingPanel, taskController, taskRoutes
- `crm_deals` + `crm_deal_tasks` — tabla, API, dealWorkflowController (parcial — ver sección 6)
- `MyDayV2.tsx` — versión duplicada de Mi Día
- Los 7+ componentes de `components/tasks/` (cuando se reescriba MyDay.tsx)

### Lo que queda — UN solo sistema llamado "Pasos"
- **Fuente base:** `follow_up_prospects` (cliente en seguimiento)
- **Templates:** `category_steps` (definidos en módulo Categorías por Gabriel)
- **Instancia por cliente:** `client_steps` — ya existe, ya tiene API en producción
- **API activa:** `GET /api/clients/:clientId/steps` y `PATCH /api/clients/:clientId/steps/:stepId`

---

## 4. CLAUDE.md — actualización completa

El archivo `CLAUDE.md` fue reescrito con:

- Corrección crítica: `server-FINAL.js` es el backend real en producción (no `app.js`)
- Criterio SQL exacto de cada tab de clientes (Activo / Cancelado / Seguimiento / Incompletos)
- Flujo completo: Seguimiento → Pasos → Comisiones → Devolver al Pool
- "Devolver al Pool" hace `is_active = false` en `follow_up_prospects` (no elimina `salesperson_id`)
- Esquemas completos de `client_steps` y `category_steps`
- Roles de usuario con origen técnico (`salespeople.role`)
- Variables de entorno por nombre
- Versiones del stack (React 19, Express 4.x, TS 5.8)
- Comando de rollback
- Tablas eliminadas documentadas para que ningún agente las recree

---

## 5. Auditoría de código — Fase 1 (solo evaluación)

### PUNTO 1 — src/backend/app.js
**Veredicto: HUÉRFANO.** Nadie lo importa. Es un refactor modular incompleto:
- Le faltan 3 rutas que sí tiene server-FINAL.js (`/my-day`, `/director`, `/tango-sync`)
- Tiene 2 rutas que no existen en server-FINAL.js (`/api/ai`, `/api/tasks`)
- El resto son equivalentes pero desincronizados

### PUNTO 2 — src/backend/ clasificado

**HUÉRFANOS confirmados (solo viven en app.js):**
- Routes: `authRoutes`, `userRoutes`, `permissionRoutes`, `aiRoutes`, `categoryRoutes`, `categoryStepsRoutes`, `categoryStepsSingleRoutes`, `clientStepsRoutes`, `followUpRoutes`, `subscriberReportsRoutes`, `tariffsRoutes`, `systemTestRoutes`
- Controllers: `aiController`, `authController`, `userController`, `permissionController`, `permissionPresetController`, `categoryController`, `tariffsController`, `followUpController`, `subscriberReportsController`, `manualComparisonController`, `validate_import_script`
- Soporte: `app.js`, `config/env.js`, `utils/responses.js`

**ZOMBIE (tablas eliminadas):**
- `routes/taskRoutes.js` + `controllers/taskController.js` (crm_tasks)
- `routes/dealWorkflowRoutes.js` + `controllers/dealWorkflowController.js` (crm_deals/crm_deal_tasks)

**IMPORTANTE — errorHandler.js NO es huérfano:**  
Lo importan `banController`, `clientController`, `importController` y otros controllers activos. Es dependencia compartida. No se toca.

### PUNTO 3 — scripts/
Carpeta casi vacía. Solo 4 archivos:
- `scripts/diagnostico/reporte-conteos.sql` — mantener
- `scripts/migraciones/add-client-notes.sql` — candidato a archivar
- `scripts/migraciones/2026-05-17-tango-sync-config.sql` — candidato a archivar
- `scripts/reset-comisiones-2026.sql` — DUDOSO (nombre peligroso, fuera de subcarpeta)

### PUNTO 4 — Módulos retirados frontend
Los 3 módulos retirados (Importador viejo, Discrepancias, Referidos) están **completamente limpios** — no hay archivos React ni rutas en App.tsx ni links en Layout.tsx.

**Zombies frontend identificados:**
- `MyDayV2.tsx` — tiene ruta activa `/mi-dia-v2` en App.tsx
- `components/tasks/` (9 archivos) — usados solo dentro de `ClientTasksPanel` y entre sí
- `ClientTasksPanel` en `Clients.tsx` líneas 16 y 5035

### PUNTO 5 — Tablas crm_*
**Hallazgo crítico:** Las tablas NO fueron eliminadas. `server-FINAL.js` línea 973 tiene `CREATE TABLE IF NOT EXISTS crm_tasks` — las recrea al arrancar. Hay contradicción con el CLAUDE.md que decía que estaban eliminadas.

**Referencias a limpiar (fase futura):**

| Archivo | Tabla |
|---|---|
| `server-FINAL.js` líneas 973–1074, 1757–2184 | crm_tasks |
| `dealWorkflowController.js` (todo el archivo) | crm_deals, crm_deal_tasks |
| `directorController.js` líneas 351–359 | crm_deal_tasks |
| `followUpController.js` líneas 222–249 | crm_deals, crm_deal_tasks |
| `myDayController.js` (SQL principal) | crm_deal_tasks |
| `goalsRoutes.js` líneas 820–866 | crm_tasks |
| `MyDay.tsx` líneas 78, 537, 620, 667, 931, 941, 1176 | crm_deal_tasks |
| `MyDayV2.tsx` (todo el archivo) | crm_deal_tasks |

---

## 6. dealWorkflowRoutes — STOP, no se toca todavía

El frontend todavía llama endpoints de dealWorkflowRoutes:

| Endpoint | Archivos |
|---|---|
| `PATCH /api/deal-tasks/:id` | `MyDay.tsx` (líneas 931, 941), `ClientTasksPanel.tsx` (líneas 326, 340) |
| `GET /api/salespeople` | `MyDay.tsx` (631), `MyDayV2.tsx` (65), `Clients.tsx` (3159) |
| `GET/POST/PUT /api/workflow-templates` | `WorkflowTemplateAdminPanel.tsx` |

**Conclusión:** `dealWorkflowRoutes.js` y `dealWorkflowController.js` se quedan hasta que se reescriban `MyDay.tsx` y `ClientTasksPanel.tsx`.

---

## 7. Limpieza Fase 1 — script aprobado

Script: `scripts/ops-deploy/cleanup-fase1.sh`

**Lo que mueve (a `.trash/$(date +%F)/`):**
- 11 controllers huérfanos del Grupo A
- 12 routes huérfanos del Grupo A
- `app.js`, `utils/responses.js`
- `taskRoutes.js` + `taskController.js` (Grupo B parcial)

**Lo que NO toca:**
- `errorHandler.js` (dependencia compartida activa)
- `dealWorkflowRoutes.js` + `dealWorkflowController.js` (frontend aún los usa)
- `server-FINAL.js` (ninguna modificación)
- Tablas en BD (no se ejecutó SQL de borrado)

**Condiciones de aprobación (Gabriel):**
1. Correr desde Git Bash o WSL, no PowerShell puro
2. Detener backend local antes (puerto 3001)
3. Revisar output de `npm run build` y arranque de `server-FINAL.js`

**Estado al cierre de sesión:** Script aprobado, pendiente de ejecución por Gabriel.

---

## 8. Pendientes para próxima sesión

### Inmediato
- [ ] Correr `cleanup-fase1.sh` y verificar build + arranque limpio
- [ ] Confirmar con `psql` si `crm_deals` y `crm_deal_tasks` siguen existiendo en producción
- [ ] Aclarar contradicción CLAUDE.md vs realidad sobre tablas crm_*

### Fase 2 (después de confirmar Fase 1)
- [ ] Reescribir `MyDay.tsx` para usar `client_steps` en lugar de `crm_deal_tasks`
- [ ] Limpiar `ClientTasksPanel.tsx` (ya no usará `/api/deal-tasks`)
- [ ] Eliminar `dealWorkflowRoutes.js` + `dealWorkflowController.js`
- [ ] Eliminar bloque `CREATE TABLE crm_tasks` de `server-FINAL.js`
- [ ] Limpiar referencias a crm_* en `directorController`, `myDayController`, `goalsRoutes`
- [ ] Eliminar `MyDayV2.tsx` y su ruta de `App.tsx`
- [ ] Eliminar carpeta `components/tasks/` y referencia en `Clients.tsx`

### Fase 3
- [ ] Redesign completo de Mi Día usando `follow_up_prospects` + `client_steps`
- [ ] Panel de informes con KPIs, metas, ranking de vendedores

---

*Generado al cierre de sesión 2026-05-24*
